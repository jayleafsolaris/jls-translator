[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "translator"
version = "1.0.1"
description = "Auto-translates Resonance of Echoes .lang files into other locales"
requires-python = ">=3.8"
dependencies = [
    "deep-translator"
]

[project.scripts]
translator = "translator.translator:main"

[tool.setuptools]
packages = ["translator"]
